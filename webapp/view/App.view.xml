<mvc:View
  controllerName="padones.santa.fe.controller.App"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m"
  xmlns:f="sap.f"
  xmlns:core="sap.ui.core"
  xmlns:unified="sap.ui.unified">
  <f:DynamicPage id="dynamicPage" class="sapUiSizeCompact">
    <f:title>
      <f:DynamicPageTitle>
        <f:heading>
          <Title text="Padrón Santa Fe - Percepciones IIBB" level="H1"/>
        </f:heading>
        <f:expandedContent>
          <ObjectStatus
            text="{= ${app>/jobStatus} ? ${app>/jobStatus} : 'Listo para cargar el padrón de percepciones' }"
            state="{= ${app>/jobStatus} === 'FINALIZADO' ? 'Success' : ${app>/jobStatus} === 'ERROR' ? 'Error' : 'Information' }"/>
        </f:expandedContent>
        <f:snappedContent>
          <Text text="Carga y procesamiento de alícuotas de percepción"/>
        </f:snappedContent>
        <f:actions>
          <Button icon="sap-icon://refresh" text="Limpiar" press=".onClear"/>
        </f:actions>
      </f:DynamicPageTitle>
    </f:title>

    <f:header>
      <f:DynamicPageHeader pinnable="true">
        <VBox class="sfHeaderContent">
          <MessageStrip
            text="Subí el archivo mensual del padrón de percepciones de Santa Fe. Se calculará la alícuota SAP según el tipo de contribuyente: C divide por 2, D conserva la alícuota original."
            type="Information"
            showIcon="true"
            class="sapUiSmallMarginBottom"/>

          <HBox wrap="Wrap" alignItems="End" justifyContent="SpaceBetween">
            <HBox wrap="Wrap" alignItems="End">
              <VBox class="sapUiSmallMarginEnd sapUiTinyMarginBottom">
                <Label text="Archivo del padrón" labelFor="fileUploader"/>
                <unified:FileUploader
                  id="fileUploader"
                  width="24rem"
                  sameFilenameAllowed="true"
                  fileType="txt,csv"
                  change=".onFileChange"
                placeholder="Seleccionar archivo de percepciones"/>
              </VBox>
              <Button
                text="Preparar carga"
                icon="sap-icon://upload-to-cloud"
                type="Emphasized"
                enabled="{= ${app>/validRows} > 0 }"
                press=".onPrepareUpload"
                class="sapUiTinyMarginBottom"/>
            </HBox>
            <HBox wrap="Wrap" class="sfKpiStrip">
              <VBox class="sfKpiItem sapUiTinyMarginBegin sapUiTinyMarginBottom">
                <Label text="Líneas"/>
                <ObjectNumber number="{app>/totalRows}" state="Information"/>
              </VBox>
              <VBox class="sfKpiItem sapUiTinyMarginBegin sapUiTinyMarginBottom">
                <Label text="Válidos"/>
                <ObjectNumber number="{app>/validRows}" state="Success"/>
              </VBox>
              <VBox class="sfKpiItem sapUiTinyMarginBegin sapUiTinyMarginBottom">
                <Label text="Advertencias"/>
                <ObjectNumber number="{app>/warningRows}" state="Warning"/>
              </VBox>
              <VBox class="sfKpiItem sapUiTinyMarginBegin sapUiTinyMarginBottom">
                <Label text="Errores"/>
                <ObjectNumber number="{app>/errorRows}" state="Error"/>
              </VBox>
            </HBox>
          </HBox>
        </VBox>
      </f:DynamicPageHeader>
    </f:header>

    <f:content>
      <VBox class="sfContent sapUiResponsiveContentPadding">
        <VBox
          class="sfResultCard sfJobSummary sapUiSmallMarginBottom"
          visible="{= !!${app>/jobId} }">
          <OverflowToolbar class="sfResultToolbar">
            <Title text="Resultado del procesamiento" level="H2"/>
            <ToolbarSpacer/>
            <ObjectStatus
              text="{app>/jobStatus}"
              state="{= ${app>/jobStatus} === 'FINALIZADO' ? 'Success' : ${app>/jobStatus} === 'FINALIZADO_CON_ERRORES' ? 'Warning' : ${app>/jobStatus} === 'ERROR' ? 'Error' : 'Information' }"/>
          </OverflowToolbar>
          <HBox wrap="Wrap" class="sfProcessKpis sapUiSmallMargin">
            <VBox class="sfProcessTile">
              <core:Icon src="sap-icon://create" color="Positive" class="sfProcessTileIcon"/>
              <Label text="Creados"/>
              <ObjectNumber
                number="{app>/createdCount}"
                state="Success"
                emphasized="true"/>
            </VBox>
            <VBox class="sfProcessTile">
              <core:Icon src="sap-icon://edit" color="Information" class="sfProcessTileIcon"/>
              <Label text="Actualizados"/>
              <ObjectNumber
                number="{app>/updatedCount}"
                state="Information"
                emphasized="true"/>
            </VBox>
            <VBox class="sfProcessTile">
              <core:Icon
                src="{= ${app>/processedErrorCount} > 0 ? 'sap-icon://error' : 'sap-icon://message-success' }"
                color="{= ${app>/processedErrorCount} > 0 ? 'Negative' : 'Positive' }"
                class="sfProcessTileIcon"/>
              <Label text="Errores"/>
              <ObjectNumber
                number="{app>/processedErrorCount}"
                state="{= ${app>/processedErrorCount} > 0 ? 'Error' : 'Success' }"
                emphasized="true"/>
            </VBox>
            <VBox class="sfProcessTile sfProcessTileWide">
              <core:Icon src="sap-icon://begin" color="Neutral" class="sfProcessTileIcon"/>
              <Label text="Inicio"/>
              <Text text="{path: 'app>/jobStartedAt', formatter: '.formatDateTime'}"/>
            </VBox>
            <VBox class="sfProcessTile sfProcessTileWide">
              <core:Icon src="sap-icon://complete" color="Neutral" class="sfProcessTileIcon"/>
              <Label text="Fin"/>
              <Text text="{path: 'app>/jobFinishedAt', formatter: '.formatDateTime'}"/>
            </VBox>
          </HBox>
        </VBox>

        <IconTabBar expandable="false" class="sapUiNoContentPadding">
          <items>
            <IconTabFilter text="Registros" icon="sap-icon://table-view" count="{app>/validRows}">
              <VBox class="sfResultCard">
                <OverflowToolbar class="sfResultToolbar">
                  <Title text="Resultados de validación" level="H2"/>
                  <ToolbarSpacer/>
                  <ObjectStatus
                    text="{= ${app>/validRows} + ' listos para procesar' }"
                    state="Success"
                    icon="sap-icon://sys-enter-2"/>
                </OverflowToolbar>

                <MessageStrip
                  text="La alícuota SAP se calcula automáticamente: tipo C divide la alícuota original por 2; tipo D conserva el valor original."
                  type="Information"
                  showIcon="true"
                  class="sapUiSmallMarginBegin sapUiSmallMarginEnd sapUiSmallMarginBottom"/>

                <ScrollContainer
                  height="34rem"
                  vertical="true"
                  horizontal="false"
                  focusable="true"
                  class="sfTableScroll">
                  <Table
                    id="recordsTable"
                    mode="None"
                    sticky="ColumnHeaders"
                    alternateRowColors="true"
                    growing="true"
                    growingScrollToLoad="true"
                    growingThreshold="50"
                    items="{app>/rows}"
                    noDataText="Cargá un archivo del padrón para ver los registros válidos.">
                    <columns>
                      <Column>
                        <Text text="Contribuyente"/>
                      </Column>
                      <Column width="8rem">
                        <Text text="CUIT"/>
                      </Column>
                      <Column width="7rem" hAlign="Center">
                        <Text text="Tipo"/>
                      </Column>
                      <Column width="10rem" hAlign="End">
                        <Text text="Alícuota Original"/>
                      </Column>
                      <Column width="9rem" hAlign="End">
                        <Text text="Alícuota SAP"/>
                      </Column>
                      <Column width="8rem" minScreenWidth="Desktop" demandPopin="true">
                        <Text text="Cliente SAP"/>
                      </Column>
                    </columns>
                    <items>
                      <ColumnListItem
                        type="Inactive"
                        highlight="{= ${app>taxpayerType} === 'C' ? 'Warning' : 'Success' }">
                        <cells>
                          <ObjectIdentifier
                            title="{app>companyName}"
                            text="{= ${app>businessPartner} ? 'BP ' + ${app>businessPartner} : 'Business Partner validado' }"/>
                          <Text text="{app>cuit}"/>
                          <ObjectStatus
                            text="{= ${app>taxpayerType} === 'C' ? 'Tipo C' : 'Tipo D' }"
                            state="{= ${app>taxpayerType} === 'C' ? 'Warning' : 'Success' }"/>
                          <ObjectNumber number="{app>originalRate}" unit="%"/>
                          <ObjectNumber
                            number="{app>rate}"
                            unit="%"
                            state="{= ${app>taxpayerType} === 'C' ? 'Warning' : 'Success' }"
                            emphasized="true"/>
                          <Text text="{app>customer}"/>
                        </cells>
                      </ColumnListItem>
                    </items>
                  </Table>
                </ScrollContainer>
              </VBox>
            </IconTabFilter>

            <IconTabFilter text="Log" icon="sap-icon://message-popup">
              <VBox class="sfResultCard">
                <OverflowToolbar class="sfResultToolbar">
                  <Title text="Registro de actividad" level="H2"/>
                  <ToolbarSpacer/>
                  <ObjectStatus
                    text="{= ${app>/messages}.length + ' eventos' }"
                    state="Information"
                    icon="sap-icon://activity-items"/>
                </OverflowToolbar>
                <List
                  items="{app>/messages}"
                  noDataText="Todavía no hay eventos registrados."
                  class="sfMessageList">
                  <CustomListItem>
                    <HBox alignItems="Start" class="sfLogItem">
                      <ObjectStatus
                        state="{app>type}"
                        icon="{= ${app>type} === 'Error' ? 'sap-icon://error' : ${app>type} === 'Warning' ? 'sap-icon://alert' : ${app>type} === 'Success' ? 'sap-icon://sys-enter-2' : 'sap-icon://hint' }"
                        class="sapUiTinyMarginEnd"/>
                      <VBox>
                        <Text text="{app>text}" wrapping="true"/>
                        <Text text="{app>timestamp}" class="sfLogTimestamp"/>
                      </VBox>
                    </HBox>
                  </CustomListItem>
                </List>
              </VBox>
            </IconTabFilter>
          </items>
        </IconTabBar>
      </VBox>
    </f:content>
  </f:DynamicPage>
</mvc:View>
